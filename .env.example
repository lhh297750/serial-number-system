# ======================================
# 序列号管理系统 - 环境变量配置文件
# ======================================
# 说明：将此文件复制为 .env 可自定义配置
# docker-compose 会自动读取 .env 文件

# ============ MySQL 数据库配置 ============
# 数据库root密码
MYSQL_ROOT_PASSWORD=root123456
# 应用数据库名称
MYSQL_DATABASE=serial_db
# 应用数据库用户名
MYSQL_USER=serial_user
# 应用数据库密码
MYSQL_PASSWORD=user123456
# 数据库端口（外部访问端口）
MYSQL_PORT=3306

# ============ 后端服务配置 ============
# 运行环境：development/production
NODE_ENV=production
# 数据库主机地址（使用Docker服务名）
DB_HOST=mysql
# 数据库端口（容器内部端口）
DB_PORT=3306
# 数据库用户名
DB_USER=serial_user
# 数据库密码
DB_PASSWORD=user123456
# 数据库名称
DB_NAME=serial_db
# JWT密钥（用于API认证）
JWT_SECRET=serial_system_secret_2024
# 后端服务端口
PORT=3000
# 允许的跨域域名（前端地址）
CORS_ORIGIN=http://localhost:8080

# ============ 前端服务配置 ============
# API基础地址（指向后端）
VITE_API_URL=http://localhost:3000/api
# 应用标题
VITE_APP_TITLE=序列号管理系统
# 前端端口（Vite开发服务器）
VITE_PORT=8080

# ============ Docker 相关配置 ============
# 数据库数据卷名称
MYSQL_VOLUME=mysql_data
# 是否启用数据库健康检查
MYSQL_HEALTHCHECK_ENABLED=true
# 是否启用服务自动重启
RESTART_POLICY=always

# ======================================
# 使用说明：
# 1. cp .env.example .env
# 2. 修改 .env 中的敏感信息（如密码）
# 3. docker-compose up -d
# ======================================

# ============ Docker Compose 配置 ============
# 后端 Dockerfile 选择（Dockerfile 或 Dockerfile.dev）
BACKEND_DOCKERFILE=Dockerfile
# 前端 Dockerfile 选择（Dockerfile 或 Dockerfile.dev）
FRONTEND_DOCKERFILE=Dockerfile.dev

# 服务端口配置（可自定义）
MYSQL_PORT=3306
BACKEND_PORT=3000
FRONTEND_PORT=8080

# 网络配置
DOCKER_NETWORK=serial-network
